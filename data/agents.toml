# Supported Agents

# OpenCode

[[agents]]
id = "opencode"
name = "OpenCode"
kind = "native"
headless_compatible = true
set_provider = true
multiple_active_providers = true
set_model = true
allow_custom_provider = true
set_provider_base_url = true
allow_custom_model = true
set_mode = true # build, plan
set_effort = true # advertised as `effort` (thought_level) when the active model has effort variants
supports_agent_skills = true
agent_skills_install_dir = "~/.agents/skills"
subagents = true
subagent_alias = "small_model"
stdio_framing = "json-lines"
website = "https://opencode.ai"
github = "anomalyco/opencode"
support_doc = "docs/agents/opencode.md"
testflight_prompt = "Create a file named .acp-stack-testflight.txt in the current workspace containing exactly: acp-stack testflight ok"
testflight_expect_fs = ".acp-stack-testflight.txt"

[agents.harness]
id = "opencode"

[agents.harness.install.shell]
script = '''
set -eu
managed_bin="${HOME}/.local/bin"
managed_opencode="${managed_bin}/opencode"
mkdir -p "$managed_bin"
# Fetch before running so a curl failure is fatal (no pipefail in POSIX sh).
# Without it an empty script runs, bash exits 0, and a stale binary left by an
# earlier run passes the postcheck — hiding the failure and skipping the npm
# fallback below, which is the path that survives GitHub API rate limits.
installer="$(curl -fsSL https://opencode.ai/install)"
# A fetch that exits 0 with an empty body would replay the same stale-binary
# postcheck hole, so an empty script is a failed fetch too.
test -n "$installer"
printf '%s' "$installer" | XDG_BIN_DIR="$managed_bin" bash -s -- --no-modify-path
test -x "$managed_opencode"
'''
creates = "opencode"
required_tools = ["curl", "bash"]

[agents.harness.install.npm]
package = "opencode-ai"
creates = "opencode"

[agents.harness.install.github]
asset_pattern = "opencode-linux-{arch}.tar.gz"
archive = "tar.gz"
binary_name = "opencode"

[agents.harness.install.github.arch]
x86_64 = "x64"
aarch64 = "arm64"

# Free auxiliary/subagent models exposed via `acps subagent free`.
#
# Resolution:
#   - If a main provider is configured, the first entry whose provider is in
#     the same family as the main provider wins. Family membership is matched
#     by provider id, or by shared canonical env ref (so `opencode-go` aliases
#     to `opencode` because both canonicalize to OPENCODE_API_KEY).
#   - If no main provider is configured, the first entry whose canonical env
#     ref is present in `[agent].env` wins. List order is the tiebreaker.
[[agents.subagent_free_models]]
provider = "openrouter"
model = "openrouter/free"

[[agents.subagent_free_models]]
provider = "opencode"
model = "opencode/big-pickle"

# Amp Code

[[agents]]
id = "amp"
name = "Amp Code"
kind = "adapter"
headless_compatible = true
set_provider = false
set_model = true # values come from the `amp-mode` Model-category option: low, medium, high, ultra (default medium)
allow_custom_provider = false
allow_custom_model = false
set_mode = true # default, bypass (permission behavior via the `permission` Mode-category option)
supports_agent_skills = true
# Amp discovers user-level skills from ~/.agents/skills natively (second in its
# skill precedence list), so no registry link directory is needed.
agent_skills_install_dir = "~/.agents/skills"
stdio_framing = "json-lines"
website = "https://ampcode.com/"
support_doc = "docs/agents/amp.md"
testflight_prompt = "Create a file named .acp-stack-testflight.txt in the current workspace containing exactly: acp-stack testflight ok"
testflight_expect_fs = ".acp-stack-testflight.txt"

[agents.adapter]
id = "amp-acp"
github = "tao12345666333/amp-acp"

[agents.adapter.install.github]
asset_pattern = "amp-acp-linux-{arch}.tar.gz"
archive = "tar.gz"
binary_name = "amp-acp"

[agents.adapter.install.github.arch]
x86_64 = "x86_64"
aarch64 = "aarch64"

[agents.harness]
id = "amp"

[agents.harness.install.shell]
script = '''
set -eu
managed_bin="${HOME}/.local/bin"
managed_amp="${managed_bin}/amp"
mkdir -p "$managed_bin"
curl -fsSL https://ampcode.com/install.sh | bash
test -x "$managed_amp"
'''
creates = "amp"
required_tools = ["curl", "bash"]

# Pi Agent

[[agents]]
id = "pi"
name = "Pi Agent"
kind = "adapter"
headless_compatible = true
set_provider = true
multiple_active_providers = true
set_model = true
allow_custom_provider = true
set_provider_base_url = true
allow_custom_model = true
set_mode = false # not supported
set_effort = true # advertised as `thought_level` (thought_level), mapping pi's thinking levels
supports_agent_skills = true
agent_skills_install_dir = "~/.agents/skills"
stdio_framing = "json-lines"
website = "https://pi.dev"
github = "earendil-works/pi"
support_doc = "docs/agents/pi.md"
testflight_prompt = "Create a file named .acp-stack-testflight.txt in the current workspace containing exactly: acp-stack testflight ok"
testflight_expect_fs = ".acp-stack-testflight.txt"

[agents.adapter]
id = "pi-acp"
github = "svkozak/pi-acp"

[agents.adapter.install.npm]
package = "pi-acp"
creates = "pi-acp"

[agents.harness]
id = "pi"

[agents.harness.install.shell]
script = '''
set -eu
managed_bin="${HOME}/.local/bin"
managed_pi="${managed_bin}/pi"
mkdir -p "$managed_bin"
# Fetch before running so a curl failure is fatal (no pipefail in POSIX sh).
installer="$(curl -fsSL https://pi.dev/install.sh)"
printf '%s' "$installer" | sh
# install.sh prefers npm's global prefix when writable (e.g. nvm hosts), so
# link that install into the managed bin the creates/postcheck paths expect.
# Also refresh links we own so a prefix move (nvm switch) can't pin a stale
# target; a real binary install.sh placed at the managed path is left alone.
if { [ ! -x "$managed_pi" ] || [ -L "$managed_pi" ]; } && command -v npm >/dev/null 2>&1; then
    if npm_prefix="$(npm prefix -g 2>/dev/null)" && [ -n "$npm_prefix" ]; then
        npm_pi="${npm_prefix}/bin/pi"
        if [ -x "$npm_pi" ]; then
            ln -sf "$npm_pi" "$managed_pi"
        fi
    fi
fi
test -x "$managed_pi"
'''
creates = "pi"
required_tools = ["curl"]

[agents.harness.install.npm]
package = "@earendil-works/pi-coding-agent"
creates = "pi"

[agents.harness.install.github]
asset_pattern = "pi-linux-{arch}.tar.gz"
archive = "tar.gz"
binary_name = "pi"

[agents.harness.install.github.arch]
x86_64 = "x64"
aarch64 = "arm64"

# Goose

[[agents]]
id = "goose"
name = "Goose"
kind = "native"
headless_compatible = true
set_provider = true
set_model = true
allow_custom_provider = true
allow_custom_model = true
set_mode = true # values come from goose's runtime advertisement (Mode-category config option)
set_effort = true # advertised as `thinking_effort` (thought_level)
supports_agent_skills = true
agent_skills_install_dir = "~/.agents/skills"
stdio_framing = "json-lines"
website = "https://goose-docs.ai/"
github = "aaif-goose/goose"
support_doc = "docs/agents/goose.md"
testflight_prompt = "Create a file named .acp-stack-testflight.txt in the current workspace containing exactly: acp-stack testflight ok"
testflight_expect_fs = ".acp-stack-testflight.txt"

[agents.harness]
id = "goose"

[agents.harness.install.shell]
script = '''
set -eu
managed_bin="${HOME}/.local/bin"
managed_goose="${managed_bin}/goose"
mkdir -p "$managed_bin"
curl -fsSL https://github.com/aaif-goose/goose/releases/download/stable/download_cli.sh | CONFIGURE=false GOOSE_BIN_DIR="$managed_bin" bash
test -x "$managed_goose"
'''
creates = "goose"
required_tools = ["curl", "bash"]

# Codex

[[agents]]
id = "codex"
name = "Codex"
kind = "adapter"
headless_compatible = true
set_provider = true
set_model = true
allow_custom_provider = true
set_provider_base_url = true
allow_custom_model = true
set_mode = true # values come from the adapter's runtime advertisement (currently e.g. read-only, agent, agent-full-access)
set_effort = true # advertised as `reasoning_effort` (thought_level) when the active model preset supports more than one effort
supports_agent_skills = true
agent_skills_install_dir = "~/.agents/skills"
stdio_framing = "json-lines"
website = "https://developers.openai.com/codex/"
github = "openai/codex"
support_doc = "docs/agents/codex.md"
testflight_prompt = "Create a file named .acp-stack-testflight.txt in the current workspace containing exactly: acp-stack testflight ok"
testflight_expect_fs = ".acp-stack-testflight.txt"

[agents.adapter]
id = "codex-acp"
github = "agentclientprotocol/codex-acp"

# Upstream moved to the agentclientprotocol org and no longer publishes
# release binaries (last zed-industries assets are v0.16.0), so install is
# npm-only like claude-agent-acp.
[agents.adapter.install.npm]
package = "@agentclientprotocol/codex-acp"
creates = "codex-acp"

[agents.harness]
id = "codex"

[agents.harness.install.npm]
package = "@openai/codex"
creates = "codex"

[agents.harness.install.github]
asset_pattern = "codex-{arch}-unknown-linux-musl.tar.gz"
archive = "tar.gz"
archive_binary_name = "codex-{arch}-unknown-linux-musl"
binary_name = "codex"

[agents.harness.install.github.arch]
x86_64 = "x86_64"
aarch64 = "aarch64"

# Claude Code

[[agents]]
id = "claude-code"
name = "Claude Code"
kind = "adapter"
headless_compatible = true
set_provider = true
set_model = true
allow_custom_provider = true
set_provider_base_url = true
allow_custom_model = true
set_mode = true # default, acceptEdits, plan, bypassPermissions
set_effort = true # advertised as `effort` (thought_level) when the active model supports effort levels
supports_agent_skills = true
agent_skills_install_dir = "~/.agents/skills"
# Claude Code only discovers ~/.claude/skills, so managed skills are symlinked there.
agent_skills_link_dir = "~/.claude/skills"
stdio_framing = "json-lines"
website = "https://code.claude.com/docs/"
support_doc = "docs/agents/claude-code.md"
testflight_prompt = "Create a file named .acp-stack-testflight.txt in the current workspace containing exactly: acp-stack testflight ok"
testflight_expect_fs = ".acp-stack-testflight.txt"

[agents.adapter]
id = "claude-agent-acp"
sync_id = "claude-acp"
github = "agentclientprotocol/claude-agent-acp"

[agents.adapter.install.npm]
package = "@agentclientprotocol/claude-agent-acp"
creates = "claude-agent-acp"

[agents.harness]
id = "claude-agent-sdk"

[agents.harness.install]
provided_by = "adapter"

# Kimi Code

[[agents]]
id = "kimi"
name = "Kimi Code"
kind = "native"
headless_compatible = true
set_provider = true
set_model = true
allow_custom_provider = true
allow_custom_model = true
set_mode = true
set_effort = true # advertised as `thinking` (thought_level): `off` plus the model's declared effort levels
supports_agent_skills = true
agent_skills_install_dir = "~/.agents/skills"
stdio_framing = "json-lines"
website = "https://www.kimi.com/code/docs/en/"
github = "MoonshotAI/kimi-code"
support_doc = "docs/agents/kimi.md"
testflight_prompt = "Create a file named .acp-stack-testflight.txt in the current workspace containing exactly: acp-stack testflight ok"
testflight_expect_fs = ".acp-stack-testflight.txt"

[agents.harness]
id = "kimi"

[agents.harness.install.shell]
script = '''
set -eu
managed_root="${HOME}/.local"
managed_kimi="${managed_root}/bin/kimi"
mkdir -p "${managed_root}"
KIMI_INSTALL_DIR="${managed_root}"
KIMI_NO_MODIFY_PATH=1
export KIMI_INSTALL_DIR KIMI_NO_MODIFY_PATH
curl -fsSL https://code.kimi.com/kimi-code/install.sh | bash
test -x "${managed_kimi}"
'''
creates = "kimi"
required_tools = ["curl", "bash"]

# Hermes Agent

[[agents]]
id = "hermes"
name = "Hermes Agent"
kind = "native"
headless_compatible = true
# Documented on the ACP agents page but absent from the upstream registry
# index; remove once upstream lists it.
sync_exempt = true
set_provider = true
set_model = true
allow_custom_provider = true
set_provider_base_url = true
allow_custom_model = true
# Hermes exposes modes only through the pre-1.0 `modes` session state, which
# this runtime does not read (ACP v1 configOptions only), so mode selection
# stays off until upstream advertises configOptions.
set_mode = false
supports_agent_skills = true
agent_skills_install_dir = "~/.agents/skills"
# Hermes only discovers ~/.hermes/skills, so managed skills are symlinked there.
agent_skills_link_dir = "~/.hermes/skills"
stdio_framing = "json-lines"
website = "https://hermes-agent.nousresearch.com"
support_doc = "docs/agents/hermes.md"
testflight_prompt = "Create a file named .acp-stack-testflight.txt in the current workspace containing exactly: acp-stack testflight ok"
testflight_expect_fs = ".acp-stack-testflight.txt"

[agents.harness]
id = "hermes"

[agents.harness.install.shell]
script = '''
set -eu
managed_bin="${HOME}/.local/bin"
managed_hermes="${managed_bin}/hermes"
hermes_home="${HOME}/.hermes"
hermes_src="${hermes_home}/hermes-agent"
mkdir -p "$managed_bin"
# Fetch to a file rather than piping into bash so the installer keeps the
# runtime's empty stdin (no prompt can consume the script body) and a curl
# failure is fatal without pipefail. The script is ~150KB and fetches in
# under a second from a healthy host, so a fetch still going at 15s means a
# dead host -- fail over to the official GitHub-hosted installer rather than
# burn the install-step budget (the Hermes release ships no binary assets,
# so a release-asset fallback is not possible).
installer="$(mktemp)"
trap 'rm -f "$installer"' EXIT INT TERM
fetch_installer() {
    curl -fsSL --connect-timeout 10 --max-time 15 "$1" -o "$installer" && test -s "$installer"
}
if ! fetch_installer https://hermes-agent.nousresearch.com/install.sh; then
    echo "vendor installer download failed; falling back to the GitHub-hosted installer" >&2
    fetch_installer https://raw.githubusercontent.com/NousResearch/hermes-agent/main/scripts/install.sh
fi
# --skip-browser does not skip the upstream Computer Use install, which has
# its own long timeout; --skip-computer-use does. --dir/--hermes-home keep
# root installs off the upstream FHS layout (/usr/local/lib + /usr/local/bin)
# so the managed paths asserted below stay accurate for every user.
bash "$installer" --skip-setup --skip-browser --skip-computer-use --non-interactive --dir "$hermes_src" --hermes-home "$hermes_home" </dev/null
test -x "$managed_hermes"
# Current installers bundle ACP mode; older ones need the optional extra
# installed into the checkout's venv, so only reach for uv when the ACP
# entry point is missing.
if ! "$managed_hermes" acp --version </dev/null >/dev/null 2>&1; then
    echo "hermes acp entry point missing; installing the optional .[acp] extra" >&2
    test -d "$hermes_src"
    uv_bin="$(command -v uv || printf '%s' "${HOME}/.hermes/bin/uv")"
    test -x "$uv_bin"
    (cd "$hermes_src" && VIRTUAL_ENV="${hermes_src}/venv" "$uv_bin" pip install -e '.[acp]')
fi
"$managed_hermes" acp --version </dev/null >/dev/null
'''
creates = "hermes"
required_tools = ["curl", "bash", "git"]

# Kilo Code

[[agents]]
id = "kilo"
name = "Kilo Code"
kind = "native"
headless_compatible = true
# No acps-written native config module exists for Kilo; auth is env-var based
# (KILO_API_KEY or provider-native keys like OPENROUTER_API_KEY), so provider
# selection stays with the harness like kimi.
set_provider = false
set_model = true
allow_custom_provider = false
allow_custom_model = false
set_mode = true # build, plan (opencode fork)
set_effort = true # advertised as `effort` (thought_level) when the active model has effort variants (opencode fork)
supports_agent_skills = true
# Kilo discovers ~/.agents/skills natively (opencode-fork skill discovery), so
# no registry link directory is needed.
agent_skills_install_dir = "~/.agents/skills"
stdio_framing = "json-lines"
website = "https://kilo.ai"
github = "Kilo-Org/kilocode"
support_doc = "docs/agents/kilo.md"
testflight_prompt = "Create a file named .acp-stack-testflight.txt in the current workspace containing exactly: acp-stack testflight ok"
testflight_expect_fs = ".acp-stack-testflight.txt"

[agents.harness]
id = "kilo"

[agents.harness.install.npm]
package = "@kilocode/cli"
creates = "kilo"

# Google Antigravity

[[agents]]
id = "antigravity"
name = "Google Antigravity"
kind = "native"
headless_compatible = true
# Upstream publishes the official ACP server as `antigravity-acp`; the catalog
# id doubles as the launch command, so it stays `antigravity`.
sync_id = "antigravity-acp"
# Auth is env-var based (GEMINI_API_KEY) against the single supported headless
# provider (Google AI Studio), selected by the `auth.type` key acps writes
# into ~/.gemini/antigravity-acp/settings.json — there is nothing to select.
set_provider = false
# session/new advertises model and mode configOptions (Gemini 3.x tiers;
# default/auto_edit/yolo). Model selection matters in practice: the default
# gemini-3.7-flash-high stalls on free-tier AI Studio keys while lower tiers
# answer.
set_model = true
set_mode = true
allow_custom_provider = false
allow_custom_model = false
supports_agent_skills = true
agent_skills_install_dir = "~/.agents/skills"
stdio_framing = "json-lines"
website = "https://antigravity.google/docs/cli/install/"
support_doc = "docs/agents/antigravity.md"
testflight_prompt = "Create a file named .acp-stack-testflight.txt in the current workspace containing exactly: acp-stack testflight ok"
testflight_expect_fs = ".acp-stack-testflight.txt"

[agents.harness]
id = "antigravity"
# Google's ACP server enters ACP mode with this literal flag rather than an
# `acp` subcommand; the upstream registry declares it for both Linux targets.
acp_args = ["--uid="]

[agents.harness.install.shell]
script = '''
set -eu
index_url="https://cdn.agentclientprotocol.com/registry/v1/latest/registry.json"
install_dir="${HOME}/.local/share/acp-stack/antigravity"
managed_bin="${HOME}/.local/bin"
launcher="${managed_bin}/antigravity"
case "$(uname -s):$(uname -m)" in
    Linux:x86_64) platform="linux-x86_64" ;;
    Linux:aarch64 | Linux:arm64) platform="linux-aarch64" ;;
    *)
        echo "Google Antigravity ships an ACP server for Linux x86_64/aarch64 only" >&2
        exit 1
        ;;
esac
mkdir -p "$managed_bin"
# Stage next to the install dir rather than /tmp: the archive is over 500MB
# and /tmp is RAM-backed tmpfs on many images.
staging_root="$(dirname "$install_dir")"
mkdir -p "$staging_root"
work="$(mktemp -d "${staging_root}/antigravity-install.XXXXXX")"
trap 'rm -rf "$work"' EXIT
trap 'rm -rf "$work"; exit 1' INT TERM
# Resolve the archive from the upstream ACP registry index instead of pinning
# a dl.google.com build tag: the tag is dated and rotates, and a shell recipe
# has no machine-checkable upstream version, so re-running install is the
# update path.
curl -fsSL --connect-timeout 10 --max-time 60 "$index_url" -o "${work}/registry.json"
archive_url="$(jq -er --arg platform "$platform" \
    '.agents[] | select(.id == "antigravity-acp") | .distribution.binary[$platform].archive' \
    "${work}/registry.json")"
# The recipe doubles as the update path (update.shell_rerun); the stamp lets
# a no-change rerun skip the 500MB+ download.
stamp="${install_dir}/.acps-archive-url"
if [ -x "$launcher" ] && [ -f "$stamp" ] && [ "$(cat "$stamp")" = "$archive_url" ]; then
    echo "antigravity archive unchanged; keeping the installed build" >&2
    exit 0
fi
# The generous cap only bounds a stalled transfer; the overall step budget
# still applies.
curl -fsSL --connect-timeout 10 --max-time 1800 "$archive_url" -o "${work}/antigravity.zip"
rm -rf "$install_dir"
mkdir -p "$install_dir"
unzip -q "${work}/antigravity.zip" -d "$install_dir"
printf '%s' "$archive_url" > "$stamp"
test -f "${install_dir}/agy_acp_server.par"
chmod +x "${install_dir}/agy_acp_server.par"
# The vendor entry point is `./agy_acp_server.par` run from the extraction
# directory, so the launcher chdir's there. ACP carries an absolute cwd per
# session, so the process cwd does not decide where the agent writes files.
printf '#!/bin/sh\ncd "%s"\nexec ./agy_acp_server.par "$@"\n' "$install_dir" > "$launcher"
chmod +x "$launcher"
test -x "$launcher"
'''
creates = "antigravity"
required_tools = ["curl", "unzip", "jq"]

# The .par ACP server has no update/upgrade subcommand and there is no npm or
# GitHub Release channel, so updating means re-running the recipe; the stamp
# check above keeps a no-change rerun cheap.
[agents.harness.update]
shell_rerun = true
