#!/usr/bin/env sh
set -eu

BROWSER_USE_VENV="${BROWSER_USE_VENV:-@BROWSER_USE_VENV@}"
BROWSER_USE_MCP_SCRIPT="${BROWSER_USE_MCP_SCRIPT:-@BROWSER_USE_MCP_SCRIPT@}"

exec "${BROWSER_USE_VENV}/bin/python" "${BROWSER_USE_MCP_SCRIPT}" "$@"
