This release moves artifact distribution to the Atrium releases service. Operator-facing behavior is unchanged.

## Improvements

- Release publication authenticates over GitHub OIDC; artifacts are checksum-verified before a release becomes visible, and publication is idempotent.
- `scripts/release.sh` prepares the version bump, changelog pairing, release commit, and tag locally.
