# Phase 5 Todo - Client And Operations Polish

All Phase 5 work is complete except the two open items below. See [docs/mgmt/roadmap.md](../../mgmt/roadmap.md) for phase context.

## Session Progress And Reconnect

- [ ] Replace the local fork message-id extension, and the per-adapter `jetbrains-air` translation beside it, when upstream ACP exposes an equivalent or richer `session/fork` shape. Both are specified in [docs/specs/acp/acp-bridge.md](../../specs/acp/acp-bridge.md), headings "Prompt Message IDs (local extension)" and "Fork Point Dialects".

## Initial Release Acceptance

- [ ] A user can install `acp-stack` on a Linux instance.
